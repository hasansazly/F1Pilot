# Node.js 24 required. All storage stays inside this project by default.
F1PILOT_DATA_DIR=./data
# Set true only behind HTTPS; false supports the local production preview.
COOKIE_SECURE=false
# No AI, email, or calendar credentials are required. External sending is disabled.
